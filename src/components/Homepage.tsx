import Login from './Login';

function login(){
    if (status === LOGGED_IN) {
        return (
            <div>
                <h1>You are logged in!</h1>
            </div>
        )
    }
}

function HomePage()
{
return(
    <>
        <div className="Homepage">
            <h1>Homepage</h1>
            
        </div>
    </>
)}
export default HomePage